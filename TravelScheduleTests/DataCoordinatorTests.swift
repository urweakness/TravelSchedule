import XCTest
@testable import TravelSchedule

final class DataCoordinatorTests: XCTestCase {
	
	func testResponse() async {
		
		let manager = 
		let monitor = await
		
		XCTAssertFalse(
			await MainActor.run {
				monitor.isConnected
			}
		)
	}
	
}
